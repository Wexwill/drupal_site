name: 'Migration from CSV file'
type: module
description: 'Provides migrations functionality for import users from SCV file.'
core_version_requirement: ^8 || ^9
dependencies:
  - migrate
  - migrate_plus
  - migrate_tools
